# vectora

Flutter client for Vectora.

## FVM

This project is pinned to Flutter 3.41.4 via FVM.

Install the SDK version for this project:

```sh
fvm install
```

Run project commands through FVM:

```sh
fvm flutter pub get
fvm flutter run
```
